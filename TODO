Move ImagePimp methods out of the AINet class
Move image methods out of ImagePimp class
Convert AINet entirely to ArrayLists so that sparse arrays and sorting can be
	handled appropriately

Cell.getClosest(ArrayList<Cell>)
Cell.getClosest(Cell[])
~                          
